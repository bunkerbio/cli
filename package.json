{
  "name": "boole-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Boole monorepo containing @boole/boole (library) and @boole/cli (CLI)",
  "scripts": {
    "build": "pnpm --filter './packages/*' run build",
    "test": "pnpm --filter './packages/*' run test",
    "lint": "pnpm --filter './packages/*' run lint"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  }
}
