/**
 * Default model configuration
 *
 * TODO: Replace with the real Boole 20B repo path + filename once uploaded to Hugging Face.
 * Current placeholder: boole-ai/boole-20b-gguf
 *
 * This model is downloaded lazily on first use, not during npm install.
 */
export const DEFAULT_MODEL = "boole-ai/boole-20b-gguf:Q4_K_M";

/**
 * TODO: Update this size once the real model is available.
 * Placeholder for first-run download message.
 */
export const DEFAULT_MODEL_SIZE_GB = "<size>";
